=== .specra/models/TC-1.json (MODEL) ===
{
  "irVersion": 1,
  "name": "The home page loads",
  "steps": [
    {
      "id": "s1",
      "sourceStepIds": [
        "ts-1"
      ],
      "action": "navigate",
      "target": {
        "page": "HomePage"
      }
    },
    {
      "id": "s2",
      "sourceStepIds": [
        "ts-2"
      ],
      "action": "assert",
      "target": {
        "page": "HomePage",
        "element": "heading"
      },
      "assertion": {
        "condition": "visible"
      }
    }
  ]
}

=== pages/HomePage.ts (PAGE) ===
import type { Locator, Page } from "@playwright/test";

/**
 * HomePage — /.
 *
 * Generated by Specra from inspected locators. Edit freely: this file is yours, and a
 * regeneration proposes a diff rather than overwriting your changes.
 */
export class HomePage {
  constructor(readonly page: Page) {}

  get heading(): Locator {
    return this.page.getByRole("heading", { name: "Welcome" });
  }

  async goto(): Promise<void> {
    await this.page.goto("/");
  }
}

=== tests/the-home-page-loads.spec.ts (SPEC) ===
import { expect, test } from "@playwright/test";

import { HomePage } from "../pages/HomePage.js";

/** TC-1 — generated by Specra from the Test Model. */
test("The home page loads", async ({ page }) => {
  const homePage = new HomePage(page);

  await homePage.goto();
  await expect(homePage.heading).toBeVisible();

});
