=== .specra/models/TC-104.json (MODEL) ===
{
  "irVersion": 1,
  "name": "Login with valid credentials",
  "description": "A registered user signs in and lands on the dashboard.",
  "tags": [
    "smoke",
    "auth"
  ],
  "parameters": [
    {
      "name": "username",
      "type": "string",
      "required": true
    },
    {
      "name": "password",
      "type": "string",
      "required": true,
      "secret": true
    }
  ],
  "steps": [
    {
      "id": "s1",
      "sourceStepIds": [
        "ts-1"
      ],
      "description": "Open the login page",
      "action": "navigate",
      "target": {
        "page": "LoginPage"
      }
    },
    {
      "id": "s2",
      "sourceStepIds": [
        "ts-2"
      ],
      "action": "fill",
      "target": {
        "page": "LoginPage",
        "element": "usernameInput"
      },
      "value": {
        "kind": "param",
        "name": "username"
      }
    },
    {
      "id": "s3",
      "sourceStepIds": [
        "ts-3"
      ],
      "action": "fill",
      "target": {
        "page": "LoginPage",
        "element": "passwordInput"
      },
      "value": {
        "kind": "secret",
        "name": "password"
      }
    },
    {
      "id": "s4",
      "sourceStepIds": [
        "ts-4"
      ],
      "action": "click",
      "target": {
        "page": "LoginPage",
        "element": "submitButton"
      }
    },
    {
      "id": "s5",
      "sourceStepIds": [
        "ts-5"
      ],
      "action": "assert",
      "target": {
        "page": "DashboardPage",
        "element": "heading"
      },
      "assertion": {
        "condition": "visible"
      }
    }
  ]
}

=== fixtures/environment.ts (FIXTURE) ===
/**
 * Test parameters, read from the environment.
 *
 * Generated by Specra. Values live in your CI secrets or your local .env — never in
 * this repository, and never in a Specra database.
 */

function read(name: string, required: boolean): string {
  const value = process.env[name];
  if (value === undefined || value === "") {
    if (required) {
      throw new Error(`${name} is not set. Add it to the environment this suite runs in.`);
    }
    return "";
  }
  return value;
}

/** Secret. Read from PASSWORD; never logged, never committed. */
export const password = (): string => read("PASSWORD", true);

export const username = (): string => read("USERNAME", true);

=== pages/DashboardPage.ts (PAGE) ===
import type { Locator, Page } from "@playwright/test";

/**
 * DashboardPage — /dashboard.
 *
 * Generated by Specra from inspected locators. Edit freely: this file is yours, and a
 * regeneration proposes a diff rather than overwriting your changes.
 */
export class DashboardPage {
  constructor(readonly page: Page) {}

  get heading(): Locator {
    return this.page.getByRole("heading", { name: "Dashboard" });
  }

  async goto(): Promise<void> {
    await this.page.goto("/dashboard");
  }
}

=== pages/LoginPage.ts (PAGE) ===
import type { Locator, Page } from "@playwright/test";

/**
 * LoginPage — /login.
 *
 * Generated by Specra from inspected locators. Edit freely: this file is yours, and a
 * regeneration proposes a diff rather than overwriting your changes.
 */
export class LoginPage {
  constructor(readonly page: Page) {}

  // confidence 0.94
  get usernameInput(): Locator {
    return this.page.getByLabel("Email");
  }

  // confidence 0.94
  get passwordInput(): Locator {
    return this.page.getByLabel("Password");
  }

  // fallback: testId login-submit
  // confidence 0.88
  get submitButton(): Locator {
    return this.page.getByRole("button", { name: "Log in" });
  }

  async goto(): Promise<void> {
    await this.page.goto("/login");
  }
}

=== tests/auth/login-with-valid-credentials.spec.ts (SPEC) ===
import { expect, test } from "@playwright/test";

import { LoginPage } from "../../pages/LoginPage.js";
import { DashboardPage } from "../../pages/DashboardPage.js";
import { username, password } from "../../fixtures/environment.js";

/** TC-104 — generated by Specra from the Test Model. */
test("Login with valid credentials @smoke @auth", async ({ page }) => {
  // A registered user signs in and lands on the dashboard.
  const loginPage = new LoginPage(page);
  const dashboardPage = new DashboardPage(page);

  // Open the login page
  await loginPage.goto();
  await loginPage.usernameInput.fill(username());
  await loginPage.passwordInput.fill(password());
  await loginPage.submitButton.click();
  await expect(dashboardPage.heading).toBeVisible();

});
